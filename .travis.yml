language: python
python:
  - "3.5"
  - "3.6"

# command to install dependencies
install:
  - pip install codecov

script:
  - coverage run Test_game_default.py

after_success:
  - codecovst # or py.test for Python versions 3.5 and below